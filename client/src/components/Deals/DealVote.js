import React from "react";

const DealVote = ({uuid, totalScore, deal_id}) => {

    // API to add vote

    // API to edit vote

    return(
        <p>Voting Here</p>
    )
}

export default DealVote;

