import React from "react";

const DealVote = ({uuid, totalVotes, userVote, dealID, }) => {

    // API to add vote

    // API to edit vote

    return(
        <p>Voting Here</p>
    )
}

export default DealVote;

