import React from "react";
import { Grid } from "@mui/material";

const FilterSortDeals = ({restaurantFilter, setRestaurantFilter}) => {

    // Update values of filter states

    // Filter and sorting components
    return(
        <Grid item xs={12}>
            <p>Filters Here</p>
        </Grid>
    )
};

export default FilterSortDeals;